export interface RAM{
    id: number;
    manufacturer: string;
    model: string;
    price: number;
    rating: number;
    photos: string[]
    speed: number
    capacity: string
    voltage: number
    moduleCount: number
    backlight: boolean
    cooling: boolean
}
